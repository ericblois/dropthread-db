/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.up = function(knex) {
  return knex.schema
    .alterTable('Item', table => {
        table.specificType('colors', 'text ARRAY').notNullable().defaultTo('{}');
    })
};

/**
 * @param { import("knex").Knex } knex
 * @returns { Promise<void> }
 */
exports.down = function(knex) {
    return knex.schema
    .alterTable('Item', table => {
        table.dropColumn('colors');
    })
};
